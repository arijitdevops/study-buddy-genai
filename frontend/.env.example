# Base URL of the Study Buddy backend.
# Leave empty to use the Vite dev-server proxy (recommended for local work).
VITE_API_BASE_URL=

# Where the dev-server proxy forwards /api requests.
VITE_PROXY_TARGET=http://localhost:8000
